//https://leetcode.com/problems/permutations/description/
//99% woohoo Actually did it myself. Feels good man
class Solution {
public:
    void addnumpermu(vector<vector<int>> &inList, const int& val)
    {
        vector<vector<int>> newList;
        
    }
    vector<vector<int>> permute(vector<int>& nums) 
    {
        vector<vector<int>> returnThis;
        returnThis.push_back({nums[0]});
        for(int i=1; i<nums.length();i++)
        {
            addnumpermu(returnThis,temp);
        }
        returnThis;
    }
};
